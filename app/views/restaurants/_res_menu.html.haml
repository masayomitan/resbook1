%ul.sidebar-list
  %li.sidebar-item
    = link_to "営業時間", time_restaurant_path, class: "sidebar-link active"
    -# %span.pull-right.text-babu
    -#   %i.fa.fa-check
  %li.sidebar-item
    = link_to "その他", other_restaurant_path, class: "sidebar-link active"
    -# - if !@room.address.blank?
      -# %span.pull-right.text-babu
      -#   %i.fa.fa-check
  %li.sidebar-item
    = link_to "戻る", new_restaurant_path, class: "sidebar-link active"

-# - is_ready = !@room.active && !@room.price.blank? && !@room.listing_name.blank? && !@room.photos.blank? && !@room.address.blank?
-# = form_for @room do |f|
-#   = f.hidden_field :active, value: true
-#   = f.submit "完了！！", id: "publish_button", class: "btn btn-normal btn-block", disabled: !is_ready
